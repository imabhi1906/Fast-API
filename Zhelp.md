to start the project start by getting the terminal inside virtual environment.
```
source venv/bin/activate
```

after that start the project with uvicorn
```
uvicorn main:app --reload
```

to stop write run command 
```
deactivate
```